# STEEL DISTRICT 작업 규칙

이 프로젝트의 공통 개발 지침이다. 현재 사용자의 명시적 요청을 우선하며, 기획·예시·실제 구현을 구분한다. 이 파일에는 지속적으로 지킬 규칙을 두고, 기능별 설계와 진행 기록은 해당 문서에 둔다.

## 1. 작업 범위와 탐색

- 요청의 목표·유지할 동작·완료 조건을 파악하고, 필요한 구현과 검증까지 진행한다. 범위 안의 통상적인 수정에 반복 확인을 요청하지 않는다.
- `rg --files`와 `rg`로 관련 경로·타입·참조를 찾고 필요한 부분부터 읽는다. 전체 프로젝트·긴 기획서·전체 씬 YAML을 매번 다시 읽지 않는다.
- 기본 소스 탐색에서 `Library/`, `Temp/`, `obj/`, `Logs/`, `.vs/`, `UserSettings/`, 빌드 결과물과 생성된 `.csproj`·`.sln`을 제외한다. 오류·패키지·검증 조사에 필요한 특정 파일만 예외로 읽는다.
- 문서와 이전 대화는 탐색 안내로 사용한다. 수정 전 실제 코드·씬·프리팹을 확인하고, 첨부 자료의 예시나 제안을 이미 구현된 기능 또는 별도 실행 지시로 간주하지 않는다.
- 관련 없는 리팩터링·패키지 업데이트·에셋 재저장을 묶지 않는다. 범위를 넓혀야 하면 발견한 의존성과 이유를 짧게 알린다.

## 2. 프로젝트 위치와 기준

- 제품 목표는 PC·Android 우선 크로스플레이, iOS 후속 지원, 저사양 기기에서도 60 FPS다. `Docs/PlatformAndPerformance.md`의 확정 요구와 미정 항목을 구분하고, 임의로 30 FPS를 완료 기준으로 낮추지 않는다. 현재 HDRP 구성을 모바일 대응 완료로 간주하지 않는다.

- 새 실행 코드는 `Assets/Code/Runtime/`의 해당 모듈, Editor 전용 코드는 `Assets/Code/Editor/`의 해당 모듈에 저장한다. 기존 네임스페이스와 모듈 구조를 따른다.
- 기본 게임 작업 씬은 `Assets/Scene/GameScene.unity`, 현재 차량 작업 대상은 그 씬의 `Simple Retro Car`다. 다른 대상이 요청되면 해당 대상을 사용한다.
- 차량 작업의 진입점은 `Docs/Vehicle.md`와 `Assets/Code/README.md`다. 실행 코드는 `Assets/Code/Runtime/`, 인스펙터와 시각화는 `Assets/Code/Editor/Vehicles/VehicleInspectors.cs`에서 확인한다.
- Unity 버전은 `ProjectSettings/ProjectVersion.txt`, 렌더 파이프라인·입력 패키지 등은 `Packages/manifest.json`과 필요 시 `Packages/packages-lock.json`으로 확인한다. 기억한 버전을 근거로 업그레이드하지 않는다.
- 새 작업에서는 `Docs/CurrentState.md`, 위치를 찾을 때는 `Docs/ProjectMap.md`, 기능 수정 시에는 해당 모듈 문서를 필요한 범위만 읽는다. 경로·책임 변경은 지도와 모듈 문서에, 구현·검증 상태 변경은 CurrentState에 반영한다. 존재하지 않는 기능이나 도구를 전제로 작업하지 않는다.

## 3. 코드와 에셋 변경

- 차량 공통 기능은 기존 모듈을 확장한다. 차종별 복제 코드를 만들거나 입력·물리·외형 효과·카메라의 책임을 한 컴포넌트에 합치지 않는다.
- 물리 힘의 적용 주기와 호출 주체를 확인한다. 기존 주행 코드가 호출하는 서스펜션 계산을 별도 `FixedUpdate`에서 중복 실행하지 않는다.
- 인스펙터 설정에는 한국어 이름 또는 툴팁, 단위, 값 변화의 의미를 제공한다. 주석은 의도와 주의할 조건을 설명한다. 반경·이동 범위 등 공간 설정은 필요할 때 선택한 대상의 Gizmo로 확인할 수 있게 한다.
- 사용자가 조정한 직렬화 값과 프리팹 오버라이드를 보존한다. 코드 기본값으로 씬 값을 덮어쓰지 않으며, 디스크에 저장된 값과 열린 Editor의 미저장 값이 같다고 가정하지 않는다.
- 공개 타입명·직렬화 필드명·스크립트 위치를 바꾸기 전 참조 영향을 확인한다. 필요하면 데이터 마이그레이션을 적용하고, 기존 `.meta` GUID와 씬·프리팹 연결을 보존한다.
- 외부 모델·재질·효과는 기존 에셋을 재사용하고 프로젝트 코드에서 확장한다. 원본 변경이 필요하면 요청과 직접 관련된 범위만 수정한다.
- 미사용 코드 삭제는 요청 범위에서 수행한다. 코드 참조뿐 아니라 씬·프리팹·에셋 GUID 참조와 동적 사용 여부를 확인하고, 삭제 후 Missing Script가 남지 않게 한다.

## 4. Unity 작업과 반복 도구

- C#은 파일로 수정한다. 씬·프리팹의 반복 변경에는 실제 사용 가능한 Editor 도구나 Unity MCP/Editor Bridge 명령을 우선한다. 연결 여부·명령·인수·반환 형식을 확인하고 없는 기능을 호출했다고 보고하지 않는다.
- 씬·프리팹 변경은 대상 경로와 변경 필드를 한정한다. 도구를 작성할 때 Undo·프리팹 오버라이드·저장을 처리하고, 재실행해도 컴포넌트나 오브젝트가 중복 생성되지 않게 한다.
- 도구가 없어 직렬화 파일을 직접 수정해야 하면 관련 블록과 GUID/fileID를 확인하고 최소 변경한다. 열린 Editor와 충돌하거나 미저장 작업을 잃을 상황에서는 먼저 충돌을 해소한다.
- 사용자 작업 씬을 검증 목적으로 덮어쓰거나 다른 씬으로 전환하지 않는다. 별도 복사본을 사용하며, 사용 중인 Unity를 임의로 종료하거나 동일 프로젝트에 두 번째 Editor를 실행하지 않는다.
- 일괄 명령은 대상·변경 전후 값·변경 개수·실패 항목을 반환하게 한다. 조회는 필요한 필드로 한정하고 전체 Hierarchy·콘솔·로그를 반복 출력하지 않는다.
- 구현된 브리지 호출과 오류 처리는 `Docs/UnityEditorBridge.md`, 반복 시험은 `Docs/TestTrack.md`를 따른다. 기본 클라이언트는 `Assets/Code/Editor/Bridge/Tools/Invoke-UnityBridge.ps1`이다. 시간 초과·INTERRUPTED 후에는 실제 결과를 확인하고 변경 명령을 새 id로 무조건 재시도하지 않는다.

## 5. 검증과 완료 보고

- 사용자 지시(2026-09-23): 토큰 절약을 위해 앞으로 주행시험을 실시하지 않는다. 사용자가 명시적으로 다시 요청하기 전에는 입력 재생·물리 시뮬레이션·수동 주행시험 및 주행을 포함한 통합 검증을 실행하지 않는다. `drive.test`, VehicleDrivingValidation, VehicleReplayValidation, BridgeValidation, BridgePlayValidation, MenuValidation의 주행 포함 진입점도 해당한다. 이를 위한 검증용 프로젝트 복사·worker 실행도 생략한다. 컴파일·정적 코드 확인·컴포넌트/참조 검사는 변경에 필요한 범위만 수행하며, 완료 보고에 주행시험 미실시를 명시한다.
- 변경과 직접 관련된 검증부터 수행한다. 관련 검증이 통과하면 새 변경·실패·미해결 의문이 없는 한 같은 검사를 반복하거나 전체 검사로 확대하지 않는다.
- 문서만 변경하면 내용·경로·규칙 충돌을 확인한다. C# 변경은 Unity 컴파일, 직렬화·컴포넌트 변경은 참조 유지, 씬·프리팹 변경은 필수 컴포넌트·Collider·레이어·Missing Script를 관련 범위에서 확인한다.
- 주행·물리 변경도 위 사용자 지시에 따라 주행시험을 생략한다. 카메라·외형·인스펙터·Gizmo의 화면 확인은 주행시험 없이 가능한 범위에서 수행하고, 확인하지 못한 부분은 명시한다. 단순 표시 변경을 위해 구현을 그대로 반복하는 테스트를 만들지 않는다.
- 기존 차량 검증은 `Assets/Code/Editor/Validation/VehicleDrivingValidation.cs`의 `SteelDistrict.Editor.VehicleDrivingValidation.RunBatch`다. 씬을 열고 Editor를 종료하는 도구이므로 **최신 변경이 반영된 별도 프로젝트 복사본의 batch mode에서만** 실행한다. 결과는 복사본의 `Logs/VehicleDrivingValidation.txt`에서 확인한다. 실행 조건은 `Docs/Validation.md`를 참조한다.
- 위 배치 검사는 수동 물리 시뮬레이션이다. 컴파일 성공·배치 검사 통과·실제 Play Mode 확인·화면 확인을 서로 대신하는 증거로 보고하지 않는다. 이전 실행 결과를 현재 변경의 검증으로 재사용하지 않는다.
- 오류는 첫 원인·파일 위치·관련 호출 경로부터 조사한다. 반복 오류는 횟수로 묶고, 같은 실패를 조건 변화 없이 재시도하지 않는다.
- 완료 보고는 한국어로 변경 내용·주요 파일·실제로 수행한 검증과 결과·미실행 검증 및 남은 문제를 간결하게 작성한다. 전체 코드를 다시 출력하지 않는다.
